# Agent rules

- Use only the Python standard library.
- Validate every external input and report record-level errors.
- Never use shell execution.
- Add or update tests before changing behavior.
- Keep modules focused and document public functions.
- Run `python -m unittest discover -s tests -v` before committing.
